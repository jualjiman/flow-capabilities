{
  "name": "flow-capabilities",
  "version": "0.0.1",
  "description": "Flow capabilities",
  "repository": "https://github.com/pitakill/flow-capabilities",
  "author": "Lepoldo Caballero <git@pitakill.net>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.5.2",
    "babel-preset-flow": "^6.23.0",
    "flow-bin": "^0.48.0"
  },
  "scripts": {
    "build": "babel src/ -d dist/",
    "test": "yarn run build && node dist/index.js",
    "test-flow": "yarn run build && node dist/index-flow.js"
  }
}
